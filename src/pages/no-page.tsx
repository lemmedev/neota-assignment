import Section from "../UI/section";

const NoPage = () => {
  return (
    <Section
      styleName={{
        width: "auto",
      }}
    >
      Please check the url
    </Section>
  );
};

export default NoPage;
